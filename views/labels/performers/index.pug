extends ../_layout.pug

block append body
  +buttonGroup.mb-4
    +buttonInGroup(
      hx-get=`/labels/${label.id}/performers/add`,
      hx-target="#modal"
    )
      i.text-xl(class="icon-[material-symbols--add]")
      span= __("Add")

  #performers(
    hx-get=`/labels/${label.id}/performers?_list`,
    hx-trigger="refresh-performers from:body"
  )
    include _list.pug
