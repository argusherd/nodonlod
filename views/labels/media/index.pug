extends ../_layout.pug

block append body
  +buttonGroup.mb-4
    +buttonInGroup(
      hx-get=`/labels/${label.id}/media/add`,
      hx-target="#modal"
    )
      i.text-xl(class="icon-[material-symbols--add]")
      span= __("Add")

  #refresh-media(
    hx-get=`/labels/${label.id}/media?_list`,
    hx-trigger="refresh-media from:body"
  )
    include _list.pug
