extends _layout.pug

block append body
  +buttonGroup.mb-4
    +buttonInGroup(
      hx-include="#form",
      hx-put=`/labels/${label.id}`,
      hx-target="#form",
      title=__("Save")
    )
      include /_mixin/button-savable.pug

  #form.mb-4
    include _info.pug
