extends ../_layout.pug

block body
  h1.mb-2.text-xl.font-bold #{ __("Media") }

  .mb-2.flex.flex-col.gap-2
    each medium in media
      include _widget.pug

  != paginator({ path: "/media", qs: qs(), count })
