include /_mixin.pug

+table.mb-4.w-full
  +tbody
    each playlist in playlists
      +tr
        +td
          .flex.flex-col.gap-2
            include /playlists/_widget.pug

            .w-20(class="sm:w-40")
              +button.mx-auto(
                hx-post=`${basePath}/playlists/${playlist.id}`,
                hx-swap="none"
              )
                i.text-xl(class="icon-[material-symbols--add]")
                span= __("Add")

+paginator(hx-target="#search-result")
