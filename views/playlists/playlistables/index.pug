extends ../_layout.pug

block append body
  +buttonGroup.mb-4
    +buttonInGroup(
      hx-get=`/playlists/${playlist.id}/media/add`,
      hx-target="#modal"
    )
      i.text-xl(class="icon-[material-symbols--add]")
      span= __("Add")

  #refresh-playlistables(
    hx-get=`/playlists/${playlist.id}/playlistables?_list`,
    hx-trigger="refresh-playlistables from:body"
  )
    include _list.pug
